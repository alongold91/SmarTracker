import React from 'react';
import { motion } from 'framer-motion';
import withTransition from '../../components/transitions/withTransition';

const UserSettings = () => {
  return <p>Expense table</p>;
};

export default withTransition(UserSettings);
